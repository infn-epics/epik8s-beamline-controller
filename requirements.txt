# Beamline Controller Requirements
# Python dependencies for the beamline controller application

# EPICS soft IOC support (pythonSoftIOC)
softioc

# Cothread for cooperative threading
cothread

# YAML parsing
pyyaml

# Ophyd for device abstraction
ophyd

# INFN Ophyd HAL (if available locally, otherwise install from source)
infn_ophyd_hal

# Logging and utilities
python-dateutil

kubernetes